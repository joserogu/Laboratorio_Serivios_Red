#ip addr add 192.168.3.2/24 dev eth0

echo "auto eth0
iface eth0 inet dhcp" >> /etc/network/interfaces

route add default gw 192.168.3.10
dhclient eth0

apt update 

# Instalamos samba

apt update && apt install samba samba-common smbclient -y

# Iniciamos servicio de samba

/etc/init.d/nmbd start
/etc/init.d/smbd start
